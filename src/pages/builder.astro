---
import Layout from '@layouts/Layout.astro';
import Header from '@components/header.astro';
import Footer from '@components/footer.astro';
import products from '@data/rawData.json';
import items from "@data/rawCarousel.json";
import Slider from '@components/subcomponents/Slider.astro';
import Filter from '@components/subcomponents/Filter.astro';
---

<Layout title={`iDwin Store - Tu tienda de confianza para iPhones reacondicionados`}
  description={`Somos iDwin Store, tu tienda de confianza para iPhones reacondicionados. Ofrecemos dispositivos de alta calidad a precios accesibles, con garantía y soporte técnico. Descubre nuestra selección de iPhones reacondicionados y encuentra el modelo perfecto para ti.`}
  image={`https://idwin.vercel.app/idwin_logo.jpg`}
  url={Astro.url.href}
  type="product"
  siteName="iDwin Store"
  locale="es_ES"
  twitterCard="summary_large_image"
  twitterCreator="@1nfinit0"
  twitterSite="@1nfinit0">
  <Header />
  <main class="w-full px-4 bg-primary flex flex-col items-center">
    <div class="w-full max-w-6xl">
      <Slider>
        {
        items.map((item) => (
          <div class=" w-full h-100 shrink-0 flex items-center justify-center snap-center relative">
            <p class="absolute top-2 left-2 text-text-feature text-4xl font-bold z-20 text-shadow-md text-shadow-white/50 m-4">{item.nombre}</p>
            <model-viewer
              src={item.src}
              environment-image="/hdri/ferndale_studio_04_1k.hdr"
              shadow-intensity="1"
              camera-controls
              auto-rotate
              class="w-full h-full"
              camera-orbit="180deg 90deg m"
            ></model-viewer>
          </div>
        ))
      }
      </Slider>
    </div>
    <Filter
  items={products}
  filters={["version", "modelo", "estado"]}
/>
  </main>
  <Footer />
</Layout>
<script>
  const params = new URLSearchParams(window.location.search);
  const category = params.get("category");
</script>